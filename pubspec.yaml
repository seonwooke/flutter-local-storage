name: flutter_local_storage
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  hive: ^2.2.3 # Hive 사용
  hive_flutter: ^1.1.0 # Hive의 추가 기능 사용
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  hive_generator: ^2.0.1 # Hive 코드 제너레이터
  build_runner: ^2.4.8 # *.g.dart 생성을 위해 사용
  
flutter:
  uses-material-design: true